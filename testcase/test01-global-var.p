program ab(cd,ef,gh);
var ij, op: integer;
function  kl(mn: integer) : integer; 
begin
   kl := ij + mn + op
end;

begin
   ij := 45;
   if ij > 1
      then op := 1
   else op := 2;
   ij := kl(55);
   printInt(ij);
end.
